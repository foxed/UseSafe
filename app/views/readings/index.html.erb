<div class="outer-section">
  <ul class="accordion-tabs-minimal">
    <li class="tab-header-and-content">
      <a href="#" class="tab-link is-active">Overview</a>
      <section>
        <%= render 'readings/overview' %>
      </section>
    </li>
    <li class="tab-header-and-content">
      <a href="#" class="tab-link">Comprehensive FAQ</a>
      <section>
        <%= render 'readings/faq' %>
      </section>
    </li>
    <li class="tab-header-and-content">
      <a href="#" class="tab-link">Basic Safety Guidelines</a>
      <section>
        <%= render 'readings/safety' %>
      </section>
    </li>
    <li class="tab-header-and-content">
      <a href="#" class="tab-link">Read More</a>
      <section>
        <%= render 'readings/read_more' %>
      </section>
    </li>
  </ul>

<script>
  $(document).ready(function () {
    $('.accordion-tabs-minimal').each(function(index) {
      $(this).children('li').first().children('a').addClass('is-active').next().addClass('is-open').show();
    });

    $('.accordion-tabs-minimal').on('click', 'li > a', function(event) {
      if (!$(this).hasClass('is-active')) {
        event.preventDefault();
        var accordionTabs = $(this).closest('.accordion-tabs-minimal')
        accordionTabs.find('.is-open').removeClass('is-open').hide();

        $(this).next().toggleClass('is-open').toggle();
        accordionTabs.find('.is-active').removeClass('is-active');
        $(this).addClass('is-active');
      } else {
        event.preventDefault();
      }
    });
  });
</script>
